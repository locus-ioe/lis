{% extends 'base.html.twig' %}

{% block title %} Locus {{ exhibition.year|date('Y') }} | {{ parent() }} {% endblock %}

{% block body %}
  <div id="wrapper">
    <div id="container">
      <div id="welcome">
        <h1>Exhibition</h1>
        <a href="{{ url('exhibitioncreate') }}"> Add</a>
        <a href="{{ url('exhibitionedit' , {'slug': exhibition.slug}) }}"> Update</a>
        <a href="{{ url('exhibitiondelete' , {'slug': exhibition.slug}) }}"> Delete</a>
        <p>{{ exhibition.id }}</p>
        <p><strong><u>{{ exhibition.theme }}</u></strong></p>
        <p>{{ exhibition.year|date('Y') }}</p>
        <p>{{ exhibition.date }}</p>
        <p>{{ exhibition.locationMap }}</p>
      </div>
      <div class="events">
        <h3>{{ exhibition.events|length }}
        {% if exhibition.events|length == 1 %}
          event
        {% else %}
          events
        {% endif %}</h3>
        <p>{% for event in exhibition.events %}
          <a href="{{ path('eventshow', {'slug': event.slug}) }}">{{ event.title }}</a>,
        {% endfor %}</p>
      </div>
      <div class="themes">
        <h3>{{ exhibition.themes|length }}
        {% if exhibition.themes|length == 1 %}
          theme
        {% else %}
          themes
        {% endif %}</h3>
        <p>{% for theme in exhibition.themes %}
          <a href="{{ path('themeshow', {'slug': theme.slug}) }}">{{ theme.name }}</a>,
        {% endfor %}</p>
      </div>
      <div class="stalls">
        <h3>{{ exhibition.stalls|length }}
        {% if exhibition.stalls|length == 1 %}
          stall
        {% else %}
          stalls
        {% endif %}</h3>
        <p>{% for stall in exhibition.stalls %}
          <a href="{{ path('stallshow', {'slug': stall.slug}) }}">{{ stall.number }} ({{ stall.size }})</a>,
        {% endfor %}</p>
      </div>
    </div>
  </div>
{% endblock %}
